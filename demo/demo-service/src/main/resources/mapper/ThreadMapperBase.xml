<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!-- auto generated by jasmine, please do not modify it! -->
<mapper namespace="com.abc.demo.repository.ThreadRepositoryBase">
    <sql id="TableName">
        tucao_thread
    </sql>

    <sql id="Columns">
     id, content, create_time, readed, replied, user_id, seller_nick
    </sql>

    <resultMap id="BaseResultMap" type="com.abc.demo.entity.Thread" autoMapping="false">
        <id property="id" column="id" />
        <result column="content" property="content" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="readed" property="readed" jdbcType="BIT"/>
        <result column="replied" property="replied" jdbcType="BIT"/>
        <result column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="seller_nick" property="sellerNick" jdbcType="VARCHAR"/>
    </resultMap>

    <insert id="insert" parameterType="com.abc.demo.entity.Thread">
        <selectKey keyProperty="id" resultType="string" order="BEFORE">
            <choose>
                <when test="id == null">
                    select replace(uuid(), '-', '') as id
                </when>
                <otherwise>
                    select #{id} as id;
                </otherwise>
            </choose>
        </selectKey>
        INSERT INTO
        <include refid="TableName"/>
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="content != null">
                content,
            </if>
            <if test="readed != null">
                readed,
            </if>
            <if test="replied != null">
                replied,
            </if>
            <if test="userId != null">
                user_id,
            </if>
            <if test="sellerNick != null">
                seller_nick,
            </if>

        </trim>
        <trim prefix="VALUES(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id,jdbcType=BIGINT},
            </if>
            <if test="content != null">
                #{content,jdbcType=VARCHAR},
            </if>
            <if test="readed != null">
                #{readed,jdbcType=BIT},
            </if>
            <if test="replied != null">
                #{replied,jdbcType=BIT},
            </if>
            <if test="userId != null">
                #{userId,jdbcType=BIGINT},
            </if>
            <if test="sellerNick != null">
                #{sellerNick,jdbcType=VARCHAR},
            </if>

        </trim>
    </insert>

    <select id="get" resultMap="BaseResultMap"
            parameterType="java.lang.String">
        select * from
        <include refid="TableName"/>
        where id = #{id}
    </select>


    <delete id="delete" parameterType="java.lang.String">
        DELETE from
        <include refid="TableName"/>
        where id = #{id}
    </delete>
    <update id="update" parameterType="com.abc.demo.entity.Thread">
        update
        <include refid="TableName"/>
        set
        <trim suffixOverrides=",">
            <if test="content != null">
                content = #{content,jdbcType=VARCHAR},
            </if>
            <if test="readed != null">
                readed = #{readed,jdbcType=BIT},
            </if>
            <if test="replied != null">
                replied = #{replied,jdbcType=BIT},
            </if>
            <if test="userId != null">
                user_id = #{userId,jdbcType=BIGINT},
            </if>
            <if test="sellerNick != null">
                seller_nick = #{sellerNick,jdbcType=VARCHAR},
            </if>
        </trim>
        where id = #{id}
    </update>

    <update id="updateColumn">
        update
        <include refid="TableName"/>
        set ${column} = #{value}
        where id = #{id}
    </update>

    <update id="updateColumns">
        update
        <include refid="TableName"/>
        set
        <trim suffixOverrides=",">
            <foreach collection="columnWithValues.keys" item="key" separator="," >
                ${key} = #{columnWithValues[${key}]}
            </foreach>
        </trim>
        where id = #{id}
    </update>
</mapper>